# login
user login-register website


project-root

│

├── index.php            # Login/Register interface

├── forgot.php           # Forgot password UI with OTP flow

├── sendotp.php          # Sends OTP to user's email

├── verifyotp.php        # Verifies OTP entered by user

├── resetpassword.php    # Updates password in database

├── config.php           # Database connection

└── assets/              # (Optional) CSS, JS, images

you must have this structure to run this code





🚀 Technologies Used
- PHP (server-side logic)
- MySQL (user database)
- AJAX (form submission)
- Bootstrap Icons (UI elements)
- Custom CSS (responsive design)
- PHPMailer / Gmail SMTP (optional for email delivery)





📌 Project Overview
Secure Red-Themed Authentication System is a responsive and visually polished web application built with PHP, MySQL, and AJAX. It provides a complete authentication flow including:
- 🔐 Login and Registration with session handling
- 🔄 Forgot Password with multi-step OTP verification
- 📧 Email-based password reset (via sendotp.php, verifyotp.php, resetpassword.php)
- 🎨 Modern red-and-white UI using Bootstrap Icons and custom CSS
- ⚙️ AJAX-powered form submission for seamless user experienc
